node_modules
dist
*.log
.DS_Store
.vite
.vscode
.env.local

# Python
__pycache__/
*.pyc
.venv/
venv/

# Scraper transient outputs — merged into data/carsandbids_master.csv,
# not committed individually.
weekly_update.*
biweekly_update.*
carsandbids_7day.*

# Backfill working files: the checkpoint is a resumable scratch log and the
# .bak is the pre-apply snapshot of the master CSV. Both are large and derived.
data/backfill_checkpoint.jsonl
data/carsandbids_master.csv.bak
data/carsandbids_master.csv.premigrate

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
